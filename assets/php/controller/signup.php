<?php

if (isset($_POST['submit'])) {
    //Grabbing data
    $fullname = $_POST['fullname'];
    $email = $_POST['email'];
    $pwd = $_POST['password'];

    //Instaintiate SignupCtr 

    //Running user handler and user signup

    //going back to front page
}