<?php

if (isset($_POST['submit'])) {
    //Grabbing data
    $email = $_POST['email'];
    $pwd = $_POST['password'];

    //Instaintiate LoginCtr 

    //Running user handler and user login

    //going back to front page
}
